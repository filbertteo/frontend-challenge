{
  "name": "frontend-challenge",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "NODE_ENV=production browserify -t [ babelify --presets [ es2015 react stage-2 ] ] main.js -o compiled.js",
    "build-dev": "browserify -t [ babelify --presets [ es2015 react stage-2 ] ] main.js -o compiled.js"
  },
  "dependencies": {
    "babel-preset-es2015": "^6.5.0",
    "babel-preset-react": "^6.5.0",
    "babel-preset-stage-2": "^6.5.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "express": "^4.13.1",
    "material-ui": "^0.14.4",
    "moment": "^2.11.2",
    "react": "^0.14.7",
    "react-dom": "^0.14.7",
    "react-tap-event-plugin": "^0.2.2",
    "request": "^2.69.0"
  }
}
